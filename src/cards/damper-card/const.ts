import { PREFIX_NAME } from "../../const";

export const DAMPER_CARD_NAME = `${PREFIX_NAME}-damper-card`;
export const DAMPER_CARD_EDITOR_NAME = `${DAMPER_CARD_NAME}-editor`;
export const DAMPER_ENTITY_DOMAINS = ["fan"];
